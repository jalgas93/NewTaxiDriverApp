import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color statusBarColor = Colors.transparent;
  static const Color primary = Color(0xff3156a2);
  static const Color primaryButton = Color(0xff457dd5);
  static const Color accent = Colors.blueAccent;
  static const Color background = Colors.white;
  static const Color scaffoldBackground = Colors.transparent;
  static const Color appBar = Colors.transparent;
  static const Color appBarIcons = Colors.white;
  static const Color appBarTitle = Colors.white;
  static const Color errors = Colors.red;
  static const Color unselectedWidget = Colors.grey;
  static const Color blockBackground = Color(0xfff8f9fd);
}
